/*
 * A primes one liner, prints out prime numbers until it runs out of stack space
 * Runs out of stack space really fast!
 * 100% legit strict C code
 * Is filled with sad faces :(
 */

int main(int x,int d)
{
    return(x==1)?main(2,0):(d?((d==x/2)?1:(x%d)&&main(x,d+1)):(printf(main(x,2)?"%i ":"",x),main(x+1,0)));
}